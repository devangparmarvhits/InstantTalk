{
  "name": "instanttalk",
  "version": "1.0.0",
  "description": "Real-time Chat Application - InstantTalk",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  }
}
